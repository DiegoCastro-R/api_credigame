const User = require("../models/Users.model");
import { IUser } from "../dtos";

const createNewUser = (UserData: IUser) => {
  /**
   * TO-DO
   * Search with user email has been already used
   * Chyper pass
   * Create User
   */
};

export default createNewUser;
